[project]
name = "transformer"
version = "0.1.0"
description = "Building the Transformer architecture from scratch"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ipykernel>=6.30.1",
    "ipywidgets>=8.1.8",
    "matplotlib>=3.10.6",
    "nbformat>=5.10.4",
    "pandas>=3.0.1",
    "plotly>=6.5.2",
    "rich>=14.3.3",
    "torch>=2.5.0",
]

[tool.uv.workspace]
members = ["venv"]
