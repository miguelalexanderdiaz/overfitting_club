[project]
name = "diffusion-transformer"
version = "0.1.0"
description = "Building a Diffusion Transformer (DiT) from scratch for image generation"
requires-python = ">=3.12"
dependencies = [
    "ipykernel>=6.30.1",
    "jupyter>=1.1.1",
    "matplotlib>=3.10.6",
    "torch>=2.5.0",
    "torchvision>=0.20.0",
    "tensorboard>=2.18.0",
]
